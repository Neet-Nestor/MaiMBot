# Alice行为问题修复总结

## 问题描述

Alice在最近的更新后出现以下问题：
1. **过度回复**：对每条消息都试图回复，即使多条消息讨论同一话题
2. **无法识别话题转换**：将新话题误认为是旧话题的延续，产生误解和争论
3. **语气不自然**：过于活跃和激进，使用过多emoji和老土的方括号表情（如【笑cry】）

## 根本原因分析

### 1. 消息路由逻辑问题 (`src/chat/heart_flow/heartFC_chat.py`)
- **独立消息处理**：每条消息都被独立评估是否回复，没有话题或上下文分组
- **简单阈值系统**：仅基于消息数量判断，threshold=1时任何单条消息都可能触发回复
- **无冷却机制**：没有追踪最近回复过的用户，导致对同一用户频繁回复

### 2. 上下文窗口过小 (`config/bot_config.toml`)
- `max_context_size` 从30减少到15，导致bot"忘记"最近的对话上下文
- 看不到足够的历史记录，无法判断话题是否已经讨论过

### 3. 配置与实现不匹配
- `plan_style` 规则说"关注当前话题，别总回到之前的内容上"，但代码没有实现话题跟踪
- 人格设定说"不是所有话题都感兴趣"，但没有话题过滤或兴趣检测系统
- 规则只是LLM提示文本，不是强制执行的逻辑

## 修复措施

### 1. 增加上下文窗口 (✅ 已完成)
**文件**: `config/bot_config.toml` (line 118)
```toml
# 修改前
max_context_size = 15

# 修改后
max_context_size = 25  # 恢复到25以改善上下文理解和话题连贯性
```

### 2. 添加消息分组逻辑 (✅ 已完成)
**文件**: `src/chat/heart_flow/heartFC_chat.py`

新增 `_should_group_with_recent_messages()` 方法：
- 检测连续快速消息（<15秒间隔）
- 等待3秒查看是否有后续消息
- 避免在用户还在打字时就抢着回复

**规则**：
- 如果最新消息距离现在<5秒，且消息间隔<15秒 → 等待更多消息
- 如果距离上次处理消息>30秒 → 认为是新话题，可以处理

### 3. 添加回复冷却机制 (✅ 已完成)
**文件**: `src/chat/heart_flow/heartFC_chat.py`

新增功能：
- `last_reply_to_user: Dict[str, float]` - 追踪每个用户最后被回复的时间
- `_check_reply_cooldown()` - 检查是否在冷却期内
- 冷却时间设置为**45秒**（适合小群）

**逻辑**：
- @提及时忽略冷却，必定回复
- 普通消息：如果45秒内已回复该用户，跳过回复
- 冷却结束后才考虑是否回复

### 4. 改进计划规则 (✅ 已完成)
**文件**: `config/bot_config.toml` (line 60)

新增规则：
```
3. **识别话题转换**：如果用户在连续多条消息中谈论同一个话题，等他们说完再回复，不要每条都回。
4. **判断是否需要回复**：不是所有消息都需要回复。如果话题不感兴趣、已经被充分讨论、或者只是闲聊，可以选择no_reply。
9. 关注当前话题，别总回到之前的内容上。如果别人在讨论新话题，就别再纠缠旧话题。
```

### 5. 修正语气和表情使用 (✅ 已完成)
**文件**: `config/bot_config.toml` (line 44)

更新 `reply_style`：
```
不要用emoji！中国女大学生很少在文字里加emoji，顶多用几个标点符号或语气词就够了（比如'？？？''……''啊''吧'）。
不要用【笑cry】【无语】这种老土的方括号表情。
```

**说明**：
- 明确禁止使用emoji和方括号表情
- 引导使用标点符号和语气词（更符合中国网友习惯）
- 表情包通过单独的emoji action发送，不在文字中混用

## 代码变更详情

### 新增类属性
```python
# 追踪最近回复的用户和时间，避免过度回复同一用户
self.last_reply_to_user: Dict[str, float] = {}  # user_id -> timestamp
self.last_processed_message_time = 0.0  # 上次处理消息的时间戳
```

### 新增方法

**1. `_should_group_with_recent_messages(messages, current_time) -> bool`**
- 判断是否应该等待更多消息
- 检测连续消息模式
- 返回True时等待3秒

**2. `_check_reply_cooldown(user_id, current_time, cooldown_seconds=60) -> bool`**
- 检查用户是否在冷却期内
- 返回True时跳过回复

**3. `_update_reply_tracking(user_id, current_time)`**
- 更新回复追踪信息
- 在实际回复后调用

### 修改的方法

**`_loopbody()` 主要变更**：
1. 添加消息分组检查，避免过早回复
2. 添加冷却时间检查（除@提及外）
3. 在回复后更新追踪信息

```python
# 新增：检查是否应该等待更多消息
if self._should_group_with_recent_messages(recent_messages_list, current_time):
    logger.debug(f"{self.log_prefix} 检测到连续消息，等待后续消息...")
    await asyncio.sleep(3)
    return True

# 新增：检查冷却时间
elif latest_user_id and self._check_reply_cooldown(latest_user_id, current_time, cooldown_seconds=45):
    logger.debug(f"{self.log_prefix} 用户 {latest_user_id} 在冷却期内，跳过回复")
    await asyncio.sleep(10)
    return True

# 新增：回复后更新追踪
if latest_user_id:
    self._update_reply_tracking(latest_user_id, current_time)
```

## 预期效果

### 问题1修复：不再对每条消息都回复
- ✅ 连续消息会等待3秒看是否有后续
- ✅ 45秒冷却期避免频繁回复同一用户
- ✅ 增大的上下文窗口让bot能看到更多历史

### 问题2修复：能识别话题转换
- ✅ 更大的上下文窗口（25条消息）
- ✅ plan_style明确要求"关注当前话题"和"识别话题转换"
- ✅ 冷却机制给其他话题留出空间

### 问题3修复：语气更自然
- ✅ 明确禁止emoji和方括号表情
- ✅ 鼓励使用标点符号和语气词
- ✅ 保持女大学生的自然语气

## 配置参数调优建议

### 根据群大小调整冷却时间
```python
# 小群（<10人）：45秒（当前设置）
cooldown_seconds=45

# 中等群（10-30人）：60-90秒
cooldown_seconds=75

# 大群（>30人）：120-180秒
cooldown_seconds=120
```

### 根据活跃度调整等待时间
```python
# 消息分组等待时间（当前：3秒）
await asyncio.sleep(3)

# 可根据群活跃度调整：
# - 非常活跃的群：2秒
# - 普通群：3秒（当前设置）
# - 不太活跃的群：5秒
```

## 测试建议

1. **消息分组测试**
   - 发送连续3条消息（间隔<10秒），验证bot等待后再回复
   - 发送单条消息，验证bot正常响应

2. **冷却机制测试**
   - 让同一用户发送多条消息，验证45秒内不重复回复
   - 测试@提及时忽略冷却

3. **话题转换测试**
   - 讨论话题A，然后切换到话题B，观察bot是否能识别并适应新话题
   - 验证bot不会"纠缠"旧话题

4. **语气测试**
   - 观察bot回复是否还在使用emoji或【笑cry】等表情
   - 验证使用标点符号和语气词代替表情

5. **上下文测试**
   - 在25条消息的历史中提到某个话题，验证bot能否记住
   - 测试话题连贯性是否改善

## 注意事项

1. **config/bot_config.toml可能在.gitignore中**
   - 配置文件修改不会出现在git status中
   - 需要手动同步到服务器或其他环境

2. **冷却时间需要根据实际情况调整**
   - 小群用45秒避免过于沉默
   - 大群可能需要更长冷却时间

3. **消息分组等待时间的权衡**
   - 等待时间太短：还是会打断连续消息
   - 等待时间太长：响应变慢，用户体验下降

4. **LLM提示的局限性**
   - `plan_style`和`reply_style`的规则只是提示，不是强制约束
   - LLM仍可能偶尔违反规则
   - 代码层面的约束（分组、冷却）更可靠

## 后续优化方向

### 1. 添加语义话题检测
- 使用embedding计算消息相似度
- 自动识别话题转换点
- 基于话题而非用户进行冷却

### 2. 兴趣度评分系统
- 根据personality设置的"喜好"自动评估话题兴趣度
- 对不感兴趣的话题降低回复概率
- 对感兴趣的话题（二次元、游戏等）提高参与度

### 3. 动态调整冷却时间
- 根据群活跃度自动调整
- 根据对话质量（被回复、被点赞等）调整
- 高质量互动时适当缩短冷却

### 4. 对话状态机
- 追踪当前对话是"热烈讨论"还是"闲聊"
- 热烈讨论时适当放宽回复限制
- 闲聊时更加克制

## 相关文件

- `src/chat/heart_flow/heartFC_chat.py` - 主要逻辑修改
- `config/bot_config.toml` - 配置调整
- `src/chat/planner_actions/planner.py` - 计划器提示（未修改，使用plan_style）

## 总结

本次修复从**代码层面**和**提示层面**双管齐下：
- 代码层面：添加消息分组、回复冷却、追踪机制
- 提示层面：明确话题识别规则、禁止emoji使用
- 配置层面：增加上下文窗口、调整冷却参数

这些修复应该能显著改善Alice的回复行为，使其更接近真实女大学生的聊天风格。
